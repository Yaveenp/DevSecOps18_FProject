apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv-k8s
  labels:
    app: postgres
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  hostPath:
    path: /mnt/data/postgres
